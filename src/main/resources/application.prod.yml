spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db_prod
    username: root
    password: Mysql#009
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  logging:
    level:
      root: ERROR